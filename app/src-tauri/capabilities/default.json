{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "store:default",
    {
      "identifier": "fs:read-text-file",
      "allow": [{ "path": "**" }]
    },
    {
      "identifier": "fs:write-text-file",
      "allow": [{ "path": "**" }]
    }
  ]
}
