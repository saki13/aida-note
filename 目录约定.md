# 目录约定

> 本文件定义 Aida 独立项目骨架的标准目录结构。新项目复制骨架后应遵循此约定。

---

## 三层分离

| 目录 | 资产类型 | 说明 |
|------|---------|------|
| `product/` | 产品资产 | 跨项目复用的规则 / 技能 / 模板 / 载体 / 白皮书 / 看板通用代码 |
| `project/` | 建设资产 | 本项目的具体产出（PCB / Backlog / SIS / Sprint / 日志 / 看板实例） |
| `memory/` | 记忆系统 | Aida 跨会话记忆（manifest / state / decisions / context_cache） |

## 单向依赖

- `product/` 不依赖 `project/`（产品资产不引用具体项目实例）
- `project/` 依赖 `product/`（建设资产按产品资产的规则 / 模板生成）
- `memory/` 只记录状态与决策，不承载产品规则

## 资产归类分水岭

- 可复用（能回流到 Aida 跨项目能力）→ 产品资产 `product/`
- 项目特定 → 建设资产 `project/`
- 详见 `product/rules/G6_资产治理模型_待确认.md`
